"use client";

/**
 * Dex Panel — PLACEHOLDER (Sprint 0).
 *
 * Dex ships at v1.5 (D-004) with graceful absence until then: no
 * entry points render in v1.0 — no stubs, no "coming soon" (XA §0.3).
 * This file reserves the feature boundary (features/dex/) and the
 * store contract (ui-store.dexOpen) so v1.5 is an insertion, not a
 * refactor. Behavior spec: specs/ai-assistant.md · docs/13 §4.14 ·
 * D-015/D-019 (represents-never-impersonates; dot, no face).
 */
export function DexPanel() {
  return null;
}
