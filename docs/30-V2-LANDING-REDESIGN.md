# V2 — Landing Redesign & Deploy

> **Status:** Planning ratified, implementation not started.
> **Created:** 2026-08-12 · **Owner:** Deepak · **Supersedes:** nothing (extends D-052/D-054/D-056)
>
> This is the single reference for the V2 development process. Every phase below
> is written to be picked up cold by a future session with no other context.
> Read §1 and §2 before touching any code.

---

## 0. How to use this document

1. **Never skip §2 (Ground Truth).** It records what actually exists today,
   verified by reading the code on 2026-08-12 — not what the older docs claim.
   Several repo docs are stale (see §2.6).
2. **Work phases in order.** §4 phases are sequenced by dependency, not by
   excitement. Phase A unblocks nothing else but is the highest user-visible
   defect; Phase C depends on B; E depends on D.
3. **Every phase has an Acceptance Gate.** A phase is not "done" until every
   box in its gate is ticked with real evidence (a command's output, a browser
   check). "It looks right" is not evidence.
4. **Open questions in §6 block their phase.** Do not guess a design decision
   that is listed there — ask.
5. **Update §7 (Progress Log) as phases complete.** That is how the next
   session knows where it is.

---

## 1. What V2 is

V2 restructures the **landing page** into the narrative the owner actually
wants a visitor to experience, fixes two live defects in the hero, and builds
the admin CRUD needed to keep it all current without editing code.

**Target landing order (top to bottom):**

| # | Beat | Status today | Phase |
| --- | --- | --- | --- |
| 1 | Hero — 3D neural face | **Broken on mobile** | A |
| 2 | Hero dive — neural network | **Wrong content + too fast** | B |
| 3 | Mission | Ships as-is | — |
| 4 | **Posts** — Featured, then Recent (5) + "More posts" | **Does not exist** | C, D |
| 5 | **Timeline** — zig-zag project spine | **Does not exist** | E |
| 6 | **Gallery** — photo cluster | Built, disabled in production | F |
| 7 | Collaborate | Ships as-is | — |

**Out of scope for V2 (explicitly):** `/projects` (the Work page) is unchanged.
Dex is unchanged. The Instrument design system is unchanged.

---

## 2. Ground truth (verified 2026-08-12)

### 2.1 Deploy

- **Live on Vercel** (moved off Render). Neon Postgres, Cloudflare R2 for media.
- `vercel.json` sets `buildCommand: npm run build --workspace=web`,
  `outputDirectory: .next`, `installCommand: npm ci`, `framework: nextjs`.
  `outputDirectory` is relative to the project's **Root Directory setting**
  (`apps/web`) — that is why it is `.next` and not `apps/web/.next`.
- `services/index.ts` wraps the DB content service in a **build-phase-only**
  fallback to file content, so an unreachable Neon at build time warns loudly
  but does not fail the deploy.

### 2.2 Landing page

`apps/web/src/app/(site)/page.tsx` currently renders exactly four beats:
`NeuralFace3DClient` → `Mission` → `Evidence` → `Collaborate`.
There is **no** GalleryStrip mounted (removed for the v1 deploy).

### 2.3 Hero / neural network

- `apps/web/src/features/hero-scene/gate.ts` — `decideTier()` returns
  Tier 0 (poster only) / 1 (mobile-capable) / 2 (desktop).
- `apps/web/src/features/hero-scene/neural-face/NeuralFace3DScene.tsx` —
  scene, bulb layers, camera rail.
- `constants.ts` — `REGION_VH = 400`, `BEAT.faceEnd = 0.32`,
  `BEAT.diveEnd = 0.60`.
- Rail is built by a **nearest-neighbour walk** through the graph
  (D-052.7 FIX 1), biased to the project/skill cluster. It is *not* ordered
  through projects.
- `labelNodes` is populated from **both** `projectNodes` and `skillNodes`, and
  the scene shows the **3 nearest** as DOM labels. This is the direct cause of
  "guided neurons are not showing my project."
- Graph data: `apps/web/public/hero-face-3d.json` — 6 `projectNodes` (published
  only), 7 `skillNodes`, 267 `ambientNodes`, generated by
  `apps/web/scripts/enrich-hero-network.ts` (`npm run hero:enrich`).
- Skills are derived from project **`tags`**, because `skillsLearned` is empty
  on every project (D-052.7 FIX 4).

### 2.4 Content model

- CTI (Content-Type Inheritance): `content_items` is the spine
  (slug, title, contentType, status, publishedAt, scheduledFor, question,
  verified); child tables extend it.
- `postsTable` exists: `dek`, `bodyMarkdown`, `readingMinutes`, `tags`.
  **There is no `featured` column.**
- `galleryItems` exists and is complete: includes `featured`, `published`,
  `sortOrder`, `altText`, `caption`, `info`, `place`, `date`, `time`,
  layout knobs (`size`, `tilt`, `depth`), `gridKey`/`fullKey` (R2 keys).
- `ContentService` (`services/content.ts`) already declares
  `getLatestPosts(limit?)` and `getPost(slug)` — both implementations exist in
  `local-content.ts` / `db-content.ts`.

### 2.5 Admin (what exists vs. what does not)

| Area | Add | Edit | Delete | Notes |
| --- | --- | --- | --- | --- |
| Projects | ✅ `createProject` | ✅ `saveProject` | ⚠️ `archiveProject` only | Also publish/unpublish/schedule/restoreVersion |
| Posts | ❌ | ❌ | ❌ | `/admin/posts` is a **stub**: "ships in the next sprint". `/admin/posts/new` 404s. No `actions/posts.ts`. |
| Gallery | ⚠️ `uploadMedia` | ❌ | ⚠️ `deleteMedia` | Media-level only. No `gallery_items` admin UI at all. |
| Publications / Skills / Timeline | ❌ | ❌ | ❌ | Route stubs only |

`archiveProject` is a **soft** delete (status change), not a row delete.

### 2.6 Stale docs — do not trust without checking

- `render.yaml`, `docs/DEPLOY_RUNBOOK.md`, `RELEASE_CHECKLIST.md` Gate 5, and
  the copy inside `admin/error.tsx` all still describe **Render**. The project
  is on Vercel. Reconciling these is Phase G.
- `memory/FEATURE_STATUS.md` lists Projects/Posts/Timeline as "Planned —
  template only"; projects are in fact substantially built.

### 2.7 Hard constraints that bind every phase

1. **Bundle budget — CI-enforced.** `scripts/check-bundle-budget.mjs` fails the
   build if `/` First Load JS exceeds **170 kB**. It was ~151 kB with four
   beats. V2 adds three content-heavy beats. **This is the single biggest
   technical risk in V2** (see §5).
2. **three.js / gsap / lenis / sharp must stay out of `/` First Load JS.**
   CI asserts this. GSAP cannot be used on the landing page.
3. **One ambient loop site-wide**, already spent on Dex's `.dl-breathe`.
   All new motion must be scroll-driven or interaction-driven.
4. **Design system closed sets**: 6 type sizes, 3 weights, 2 easings
   (`--ease-instrument`, `--ease-arc`), one accent gradient used as energy only.
   Additions require a `DECISIONS.md` entry.
5. **LAW-008 honesty**: empty fields self-hide; never fabricate a label.
   Empty `alt` text is a release blocker, not a cosmetic issue.
6. **`prefers-reduced-motion`** must render the final state, never mid-animation.

---

## 3. Sequencing

```
A. Hero mobile fix ──────────────┐
B. Neural network redesign ──────┤ (independent; both are hero defects)
                                 │
C. Posts data + admin CRUD ──────┼──► D. Posts on landing + /posts page
                                 │
E. Timeline section ─────────────┤ (depends on projects data, already live)
                                 │
F. Gallery re-enable ────────────┤ (depends on owner copy pass)
                                 │
G. Docs reconciliation ──────────┘ (any time)
H. Bundle budget reconciliation ─── (continuous; verify after C–F)
```

**Recommended order:** A → B → C → D → E → F → G, with H checked after every
phase that touches `/`.

Rationale: A and B are live defects on the page every visitor sees first —
they outrank new features. C must precede D (no data, nothing to render).
F is last because it is gated on owner copy, not engineering.

---

## 4. Phases

### Phase A — Hero renders on mobile

**Defect:** the hero animation is not visible on any mobile device; the poster
stays.

**Hypotheses, in order of likelihood.** These are *candidates to verify*, not
confirmed causes — Phase A begins with diagnosis, not with edits.

1. **`failIfMajorPerformanceCaveat: true`** in `probeWebGL2()` (`gate.ts:40`).
   This flag makes browsers return `null` on many mobile GPUs and in software-
   rendering situations even when WebGL2 is perfectly usable. Strongest
   suspect — it single-handedly forces Tier 0 (poster, permanently).
2. **`deviceMemory < 4` → Tier 0** (`gate.ts:22`). Chrome on Android commonly
   reports `2` or `4` on mid-range devices. A `4` passes; a `2` does not. This
   would knock out a large share of real phones.
3. **`h-svh` sticky scroll region.** The hero uses `sticky top-0 h-svh` inside a
   `REGION_VH = 400` region. Mobile URL-bar collapse changes the viewport unit
   mid-scroll, which can break sticky positioning and make the scroll-driven
   beats never advance.
4. **Tier 1 asset path.** Tier 1 loads a 2500-node mobile dataset. Confirm the
   mobile branch actually fetches and mounts rather than silently failing.

**Work:**
- Add a temporary diagnostic (dev-only) that surfaces the tier decision and the
  reason on-device — do not guess from a desktop emulator alone.
- Verify on a **real** device (or BrowserStack), not just Chrome DevTools device
  mode: DevTools does not reproduce `deviceMemory` or GPU-caveat behaviour.
- Fix the identified cause(s). Expected shape: drop
  `failIfMajorPerformanceCaveat`, lower/remove the `deviceMemory` threshold, and
  replace `svh` with `dvh` (or a JS-measured height) if #3 is confirmed.
- Keep the poster fallback ladder intact — it must still work for genuine
  Tier 0 devices and for reduced-motion.

**Acceptance Gate A:**
- [ ] Root cause identified and written into this doc's §7, with the evidence.
- [ ] Hero animates on at least **two** real phones (one Android, one iOS).
- [ ] Reduced-motion still shows the poster only, on mobile and desktop.
- [ ] A device that genuinely fails WebGL2 still gets the poster (no black box).
- [ ] `npm run check:bundle --workspace=web` still passes.

---

### Phase B — Neural network guides through projects

**Defect (owner-reported):** the guide moves too fast on scroll, and the
highlighted neurons show "different stuff" rather than projects.

**Confirmed cause of the wrong content:** `labelNodes` is built from *both*
`projectNodes` and `skillNodes` (`NeuralFace3DScene.tsx` ~554–570), and the
scene labels the 3 nearest. Skills therefore compete with — and often beat —
projects for the label slots.

**Target behaviour (owner spec):**
- The camera rail passes **through the project neurons in a deliberate order**.
- Each project neuron is the labelled focus while the camera is near it.
- Neurons *not* on the guided path are **skills** used in those projects —
  agentic AI, LLMs, prompt engineering, and similar.
- **Low-level / foundational skills appear at the start and the end** of the
  neural-network scroll.

**Work:**
1. **Rail redesign** — replace the nearest-neighbour walk with an ordered path
   over `projectNodes`. Ordering should be **chronological** so the dive tells
   the same story as the Timeline (§E). Requires project dates in the graph
   data.
2. **Label policy** — label **projects only** while on the guided path.
   Skills may still render as nodes (and may label on close proximity), but must
   never displace the current project.
3. **Dwell + pacing** — the "too fast" complaint is a pacing problem. Two levers:
   (a) add a dwell/ease-out at each project waypoint so the camera settles
   rather than sweeping past; (b) raise `REGION_VH` above 400 to buy scroll
   distance. Prefer (a) first — lengthening the region makes the page feel long
   without fixing the sensation of being rushed.
4. **Skill placement** — regenerate the graph so foundational skills sit at the
   spatial start and end of the rail. This is a change to
   `scripts/enrich-hero-network.ts` and a `npm run hero:enrich` regeneration.
5. **Skill taxonomy** — skills currently come from project `tags`, which are
   tech tags, not the agentic-AI vocabulary the owner wants. Either populate
   `skillsLearned` on projects (preferred — it also fixes D-052.7 FIX 4's
   honest limitation) or introduce a curated skill list. **See Open Question 3.**

**Acceptance Gate B:**
- [ ] Scrolling the dive visits every published project in chronological order.
- [ ] The labelled node while on the path is always a project, never a skill.
- [ ] Foundational skills are visible at both ends of the dive.
- [ ] Camera motion has no frame-to-frame jump above the D-052.7 measured
      ceiling (max step ≤ 0.055).
- [ ] Fast scrolling does not skip past projects without them registering.
- [ ] Regenerated `hero-face-3d.json` still within its gz budget (≤160 kB).
- [ ] `check:bundle` passes.

---

### Phase C — Posts: data model + admin CRUD

**Everything here is net-new.** `/admin/posts` is a placeholder.

**Work:**
1. **Schema** — add `featured boolean not null default false` to `postsTable`.
   Generate a Drizzle migration; apply to local and to Neon.
   Consider `featuredOrder integer` if more than one featured post is allowed
   (**Open Question 4**).
2. **`actions/posts.ts`** — server actions mirroring `actions/projects.ts`:
   `createPost`, `savePost`, `publishPost`, `unpublishPost`, `deletePost`,
   `toggleFeatured`. Follow the existing CTI write pattern (write
   `content_items` + `posts` in one transaction) and the existing
   `content_versions` snapshot convention.
3. **Admin UI** — `/admin/posts` list (with status + featured indicator),
   `/admin/posts/new`, `/admin/posts/[slug]` editor. Reuse the projects editor
   patterns rather than inventing new ones.
4. **The Feature toggle** — a switch in the post editor that sets `featured`.
   Must be obvious in the list view which post is featured.
5. **Delete semantics** — projects only soft-delete (`archiveProject`). The
   owner asked for *delete*. **See Open Question 5.**

**Acceptance Gate C:**
- [ ] Migration applied locally **and** on Neon; `\d posts` shows `featured`.
- [ ] A post can be created, edited, published, unpublished, featured, and
      deleted entirely from `/admin/posts`.
- [ ] Featured state survives a reload and is visible in the list.
- [ ] `npm run typecheck` clean; admin bundle still absent from public
      First Load JS (CI guard).

---

### Phase D — Posts on the landing page + `/posts`

**Depends on:** Phase C.

**Target behaviour (owner spec):**
- A **Featured** block (LinkedIn-style) directly after the neural network.
- Then a **Posts** block: up to **5** recent posts, scrollable.
- A **"More posts"** action → `/posts`, listing everything.

**Work:**
1. `getFeaturedPosts()` on `ContentService` (both file and db implementations),
   plus `getLatestPosts(5)` which already exists.
2. Landing sections: `FeaturedPost` and `RecentPosts` under
   `features/posts/`. **Server components** — no client JS unless a phase gate
   proves it necessary (bundle budget, §2.7).
3. `/posts` page + `BUILT_ROUTES` registration + footer link.
4. Empty states: if there are no posts, the sections **self-hide** (LAW-008).
   No "coming soon" placeholders.
5. Markdown rendering for `bodyMarkdown` — pick a renderer with a known bundle
   cost, and render on the **server** so it costs `/` nothing.

**Acceptance Gate D:**
- [x] Featuring a post in admin makes it appear in the landing Featured block.
- [x] Recent block shows at most 5, newest first, published only.
- [x] "More posts" reaches `/posts`, which lists all published posts.
- [x] With zero posts, both sections vanish and the page still reads correctly.
- [x] `/` First Load JS still ≤ 170 kB (`check:bundle`).

---

### Phase E — Timeline section

**Does not touch** `/projects`, which stays exactly as it is.

**Amended target behaviour (owner decisions, 2026-08-13 — supersede the
original spec above):**
- A **vertical zig-zag linked list**; each node is a project block.
- Shows project name, `problem` one-liner, and active/archived status —
  same info already on the project, no new content required.
- Scroll-driven reveal.
- **Ordering is owner-assigned, not chronological.** `start_date`/`end_date`
  are NULL on every project and `year` is 2026 for all 6 published ones —
  there is no real date signal to order by yet, and `projects.txt` (cited in
  the original spec) doesn't exist in the repo. The owner manages a numeric
  **Timeline position** field per project in admin instead (mirrors
  `featuredOrder`'s pattern); a project only joins the Timeline once that's
  set, and only in the position assigned — this is "dynamic" in the sense
  that no code/deploy is needed, but it is not automatic on publish alone.
- **No dates are rendered** — showing a fabricated month (all 6 projects'
  only date signal, `published_at`, is literally the 1st of six consecutive
  months) would violate LAW-008. Order alone tells the story.
- Clicking a node → `/projects/[slug]` for that specific project (resolves
  Open Question 6).
- **`Evidence` is NOT removed** — Timeline mounts alongside it (Posts →
  Timeline → Evidence → Collaborate). Open Question 1 is deliberately left
  open; deleting a ratified section needs an explicit owner call, not an
  inference from "start the work".

**Work:**
1. Schema: `projectsTable.timelineOrder integer` (nullable), migration
   `0009_light_logan.sql`. `ContentService.getTimelineProjects()` —
   published, `timelineOrder IS NOT NULL`, ordered ascending.
2. Admin: "Timeline position" numeric field in `ProjectEditor.tsx`,
   `saveProject` persists it. Public-path (`/`) revalidation added to
   `saveProject`/`publishProject`/`unpublishProject`/`archiveProject`/
   `restoreVersion` — the Phase D revalidation gap, pre-empted this time.
3. Layout — zig-zag alternating left/right with a connecting spine (CSS
   Grid, mobile-first; single column + left spine below 768px, no
   `display: contents`). Whole node is one link; semantic `<ol>`.
4. Motion — scroll-driven reveal per node (`animation-timeline: view()`),
   direction matches which side the card is on; mirrors the gallery
   cluster's `@supports`/base-state-is-final-state pattern exactly, so
   reduced-motion and unsupported browsers need no separate override.

**Acceptance Gate E:**
- [x] Every project with a Timeline position appears as a node, in the
      owner-assigned order (not chronological — see above).
- [x] Setting a Timeline position in admin makes the project appear without
      a code change or redeploy (public-path revalidation).
- [x] Zig-zag on desktop, single column on mobile, spine unbroken at both.
- [x] Clicking a node navigates to `/projects/[slug]` correctly.
- [x] Reduced-motion / no `animation-timeline` support shows all nodes in
      final state (verified by construction — the final state is the base
      style, motion is additive only inside the supported+no-preference
      case, not a separate override to remember).
- [x] `check:bundle` passes.

---

### Phase F — Gallery re-enable

**Amended 2026-08-13 — the original framing above was wrong about where the
photos live, and the "owner copy pass" dependency dissolved.**

The pre-2026-08-13 plan assumed 11 file-backed photos needing an alt-text pass.
Verified against production before starting: **`gallery_items` had 0 rows.**
Those 11 photos exist only in `content/gallery.ts` (file mode); production runs
`CONTENT_SOURCE=db`, so removing the `notFound()` guard alone would have shipped
an empty page. Meanwhile the media library held **19 images, all with alt text
and dimensions** — media requires alt on upload. So the real gap was never copy,
it was that there was no way to *put* an image into the gallery.

**Owner decision (2026-08-13):** build a `Gallery` admin section that curates
gallery items from existing media, and go live in the same phase.

**The media → gallery_items gap.** `gallery_items` predates this flow and wants
things `media` doesn't carry:

| `gallery_items` needs | `media` has |
| --- | --- |
| `gridKey` + `fullKey` (two renditions) | one `storageKey` |
| `blurData` (**notNull, no default**) | — |
| `width`/`height`/`orientation` | width/height ✓ |

Resolved by: pointing both keys at the single media object (`next/image` already
serves a right-sized rendition per breakpoint via `sizes`, so a second stored
copy buys nothing), and generating `blurData` with `sharp` at add-time. Blur
cannot be deferred — all three gallery components pass it to
`placeholder="blur"`, which throws on an empty value. `sharp` already runs on
this exact runtime path (`lib/media/validate.ts`, called by `uploadMedia`), so
it is a proven dependency here, not a new risk.

**Work:**
1. `actions/gallery.ts` — `addToGallery` (R2 fetch → sharp blur → orientation →
   unique slug → append to order), `saveGalleryItem`, `removeFromGallery`,
   `moveGalleryItem`. Every mutation revalidates `/admin/gallery`, `/gallery`
   and `/`.
2. `/admin/gallery` + `GalleryManager` — picker of media images not yet in the
   gallery, then per-item editing of alt/caption/info/place/date/time, the
   layout knobs (`size`/`tilt`/`depth`), order, and Published/Featured.
   Registered in `AdminShell`'s nav.
3. **`services/gallery.ts` now filters `published = true`** — it previously
   returned every row regardless, which would have made the Published toggle
   decorative and leaked drafts publicly the moment the guard came off.
4. Remove the production `notFound()` guard; `/gallery` renders an honest
   `EmptyState` at zero photos rather than 404ing (matching `/posts`), since
   it is now in `BUILT_ROUTES` and a footer link that 404s is a fabricated
   affordance (LAW-008).
5. Mount `GalleryStrip` on the landing after Timeline; `ROUTES.gallery` added
   to `BUILT_ROUTES` so the footer link appears via the existing mechanism.
6. Positional-ID problem (**Open Question 7**) — dissolved for DB-mode items:
   slugs are now derived from alt text with a uniqueness suffix and stored, so
   adding a photo can never renumber an existing one. The `g01…gNN` scheme
   survives only in the file-mode fixture.

**Acceptance Gate F:**
- [x] Every gallery photo has non-empty `alt` — enforced at two levels: media
      requires alt on upload, and `saveGalleryItem` rejects an empty value.
- [x] `/gallery` reachable in production (guard removed).
- [x] Landing cluster renders after Timeline; "More images" works.
- [x] Deep links resolve — slugs are stored per item, not positional.
- [x] Unpublished items appear nowhere public (verified by flipping a real row
      to `published=false` and rebuilding).
- [x] `check:bundle` passes, and confirms `sharp` stayed out of `/`'s client
      bundle despite the new server-action import.

---

### Phase G — Docs reconciliation

The repo still documents a Render deploy (§2.6). Leaving it is exactly the kind
of stale-doc trap that has already cost this project time.

- [ ] `render.yaml` — delete, or mark clearly as superseded.
- [ ] `docs/DEPLOY_RUNBOOK.md` / `docs/10-DEPLOYMENT.md` — rewrite for Vercel +
      Neon + R2.
- [ ] `RELEASE_CHECKLIST.md` Gate 5 — Vercel env vars, not Render.
- [ ] `admin/error.tsx` production copy — says "Render dashboard".
- [ ] `memory/FEATURE_STATUS.md` — correct the Projects/Posts/Timeline rows.
- [ ] `memory/CURRENT_STATE.md` + `DECISIONS.md` — record V2 as D-058.

---

## 5. The bundle budget risk

**This is the most likely way V2 fails technically.**

`/` was ~151 kB against a hard 170 kB CI ceiling with four beats. V2 adds
Posts (×2 blocks), Timeline, and Gallery. Roughly **19 kB of headroom** for
three feature areas.

**Mitigations, in preference order:**
1. **Server components by default.** Posts, Timeline and the Gallery strip can
   all render server-side. The gallery cluster already proves a rich,
   scroll-animated section can cost **0 kB** of client JS by using CSS
   `animation-timeline: view()`.
2. **CSS-driven motion, never a JS animation library.** GSAP is banned from `/`
   by CI anyway.
3. **Server-side markdown.** Never ship a markdown parser to the browser.
4. **Measure after every phase**, not at the end. `check:bundle` after C, D, E
   and F individually — discovering a 25 kB overshoot at the end means not
   knowing which of three features caused it.

**If the ceiling is genuinely reached:** raising it is a `DECISIONS.md`-level
change, not a quiet edit to the guard. Justify it with measured LCP/TBT impact.

---

## 6. Open questions — these block their phases

| # | Question | Blocks | Why it matters |
| --- | --- | --- | --- |
| 1 | ~~Does **Evidence** get deleted, or move elsewhere?~~ — **Answered 2026-08-13.** Not deleted. Timeline mounts alongside it (Posts → Timeline → Evidence → Collaborate); owner said "start the work" but never confirmed deletion, and it's a not-reversible-by-accident call, so it stayed. Owner can revisit any time. | E | Resolved — see D-058 Phase E entry. |
| 2 | Where do **Mission** and **Collaborate** sit in the final order? | E | The owner's stated order omits them; assuming is risky. |
| 3 | ~~**Skill taxonomy**~~ — **Answered 2026-08-13.** Owner provided the real list directly (Languages; AI/ML; Core CS; AI & Automation; Systems & Backend; Tools). Implemented in `enrich-hero-network.ts`: `HERO_SKILL_ADDITIONS` maps real, code-audit-evidenced skills onto the specific projects that use them (e.g. Agentic AI/Multi-Agent Systems/AI Agents → asmos; PyTorch/Computer Vision/Deep Learning/Transformers → turb-detr); `FOUNDATIONAL_SKILLS` (Data Structures & Algorithms, Operating Systems) are the named start/end bookends; `GENERAL_AI_SKILLS` (Prompt Engineering, RAG concepts, AI Workflows, GenAI Tools) render as unconnected atmosphere nodes. 22 skill nodes total (was 7). Does not touch `content/site.ts`'s stored `tags` — hero-graph-only. | B | Resolved — see D-058 Phase B entry below. |
| 4 | ~~**How many featured posts?**~~ — **Answered twice, 2026-08-13.** First: one at a time (`featured` boolean, Phase C). Revised same day after the owner researched LinkedIn's post UI and asked for a carousel: several, ordered (`featuredOrder` integer, migration split into two unambiguous steps since drizzle-kit's rename-vs-recreate prompt needs a TTY this session didn't have). | C, D | Resolved — see D-058 Phase D carousel-redesign entry. |
| 5 | **Delete = hard or soft?** Projects currently only archive. Owner said "delete". | C | Hard delete is irreversible and loses version history. Recommend soft-delete + an "Archived" filter, but this is the owner's call. |
| 6 | ~~Timeline node click → `/projects` (index) or `/projects/[slug]`?~~ — **Answered 2026-08-13.** Owner confirmed `/projects/[slug]` (that specific project). | E | Resolved — see D-058 Phase E entry. |
| 7 | ~~Gallery **stable IDs**~~ — **Resolved 2026-08-13 by construction.** DB-mode gallery items store a slug derived from their alt text (with a `-2`, `-3`… suffix on collision) at add time, so adding a photo can never renumber an existing one. The positional `g01…gNN` scheme survives only in the file-mode fixture, which production doesn't read. | F | Resolved — see D-058 Phase F entry. |
| 8 | **Timeline visual design** — owner said they will share a reference image. **Never arrived.** Owner said "start the work" anyway; Phase E shipped to the Instrument system defaults (no card chrome, restrained per Mission/Evidence's existing aesthetic) rather than block on it. If a reference image arrives later, revisit the visual treatment then — the data/ordering/link-target work underneath doesn't change. | E | No longer blocking — owner explicitly chose to proceed without it. |

---

## 7. Progress log

> Append one entry per phase completion. Include what was verified and how.

| Date | Phase | Outcome | Evidence |
| --- | --- | --- | --- |
| 2026-08-12 | — | Plan written. Ground truth verified by code read. | This document §2 |
| 2026-08-13 | A | **Done.** Two real causes, not the two original hypotheses: (1) `failIfMajorPerformanceCaveat: true` in the WebGL2 probe — removed. (2) Mount itself was fine; the mobile surface portrait rendered at ~10% of desktop's visual coverage (2500/8000 points × smaller `uSize` × capped DPR, coverage scales with size²) — arithmetically near-invisible, not broken. Fixed via `?herodebug=1` real-device evidence across 3 iterations: `uSize` matched to desktop (4.2), then `mobileCount` 2500→5000→8000 (full parity). Owner confirmed fixed on real device (Android, `deviceMemory:8`, Chrome 150). `svh`/`vh` mismatch hypothesis was never confirmed or needed — not touched. | Commits `c9ffa79`, `9701128`, `1e2be0d`, `1bb9a1c`. Owner: "done". |
| 2026-08-13 | B | **Structural + vocabulary done. Visual feel not yet verified.** Confirmed root cause: `updateLabels` ranked project AND skill nodes together by raw distance — a skill regularly won the label slot over a project. Rewrote `buildFlightRail` to walk a caller-supplied ORDER (was: nearest-neighbour by distance) with per-segment bridging so non-adjacent chronological neighbours stay within the D-052.7 smoothness ceiling; added a loiter cluster per waypoint for dwell (the "too fast" fix, no `REGION_VH` change); extracted the original nearest-neighbour walk into `nearestNeighbourWalk()` so the homogeneous (no-`network`) fallback path keeps its old behaviour instead of degenerating to a single-point rail — caught in review before shipping. Threaded `publishedAt` through `enrich-hero-network.ts` (silently dropped by `ProjectRecord` on the first attempt — caught before it shipped). Labels filtered to `kind === "project"` **plus the two named bookend skill nodes** (they need to be readable, not just present). Open Question 3 resolved same day — owner provided the real skill list; `HERO_SKILL_ADDITIONS`/`FOUNDATIONAL_SKILLS`/`GENERAL_AI_SKILLS` added to the generator (22 skill nodes, was 7), bookends now selected **by name** ("Data Structures & Algorithms" / "Operating Systems"), not just by connectivity. Verified by simulating the full sort+bookend+label logic against the real regenerated `hero-face-3d.json`: chronological PESU Vault→Turb-DETR→Docksmith→ShortcutScore→Dental AI→ASMOS (Jan–Jun 2026), bookended and labelled `Data Structures & Algorithms` / `Operating Systems`, off-path nodes show the real evidenced vocabulary (Agentic AI/Multi-Agent Systems/AI Agents near ASMOS, etc.) plus general AI/automation terms as unconnected atmosphere. Typecheck, full build, and bundle guard all pass (`hero-face-3d.json` 45.5 kB, `/` unaffected at 151.7 kB). **Not yet verified:** actual on-screen motion/pacing feel and label readability during a real scroll — needs owner review in-browser, not just logic simulation. | Commit `518b0df`. Logic verified via `node -e` against real regenerated data; not yet browser-verified. |
| 2026-08-13 | B (dwell re-tune) | **Owner tested in-browser: "still too fast."** First dwell attempt (1 lap, 3 points, radius 0.18×walkStep) was real but too small: measured against the actual dataset it added only ~0.17 of arc length per waypoint vs ~0.69 of transit — dwell was ~20% of the journey, not enough to register. Raised to `LOITER_LAPS=2` full revolutions, `LOITER_STEPS_PER_LAP=5`, radius `0.32×walkStep`. Measured (not estimated) against the real regenerated dataset: total rail arc length +92% (0.86 → 1.66), dwell-specific portion 5.1×. Caught in review before shipping: the larger radius pushed one transition (last bridge point → first loiter point, which starts already offset sideways by the radius) to ~9% over the D-052.7 smoothness ceiling — fixed by visiting the bare waypoint centre before entering the loiter loop, so every step is either a normal bridge hop or a small centre↔loiter-point radius, never both compounded. Re-measured after the fix: zero steps over ceiling, max step 0.0333 vs 0.0366 ceiling. Ambient background glow behind the hero also raised ~12–15%→~20–25% peak opacity same session (owner: "reduced, increase it") — `docs/DESIGN_SYSTEM.md`'s ratified value updated to match. Typecheck, full build, bundle guard all pass. | Commit pending. Arc-length and ceiling-compliance verified via `node --input-type=module` against real regenerated data; on-screen feel still needs owner's eyes. |
| 2026-08-13 | C | **Done.** Posts data model + admin CRUD, net-new (`/admin/posts` was a stub, `/admin/posts/new` 404'd). `postsTable.featured boolean not null default false` added (single boolean, one-at-a-time — not `featuredOrder`, per owner decision); migration `0006_bright_moondragon.sql`. `actions/posts.ts` mirrors `actions/projects.ts`'s CTI write pattern: `createPost`, `savePost` (transaction unsets `featured` on every other post whenever this one is set featured), `publishPost`, `unpublishPost`, `archivePost` (soft delete, `origin: "unpublish"` — same convention as `archiveProject`), `restorePostVersion`. Admin UI: list (Active/Archived tabs, status + ★ Featured column), new, editor, version history. Found two real bugs during the work: (1) `ProjectEditor.tsx`'s metadata aside sits outside its `<form>` with no `form=` attribute, so slug/year/status/tags are silently dropped on every project save — pre-existing, left alone (protected file), flagged as a background task; fixed the equivalent in `PostEditor.tsx` before shipping. (2) The posts list's "Archived" filter tab initially dropped the WHERE clause instead of flipping it, showing every post — caught during live verification, fixed same session. Verified against a real local Postgres, not simulated: `\d posts` shows `featured`; full browser click-through created two posts, edited one (all fields persisted, confirming the aside-outside-form fix), published, un-published, featured post A, then featured post B and confirmed A flipped back to `featured=false` in the same query (the case flagged as most likely to be buggy), archived post B (excluded from Active, shown under Archived), reloaded the list and confirmed exactly one ★ survives reload, and exercised `restorePostVersion`. Spot-checked `/admin/projects` (list + `asmos` editor) against real project data — untouched, works. `npm run typecheck --workspace=web` clean. | Commit `2fbe8c5`. Neon migration pending human action — not run from this session (see D-058 entry). |
| 2026-08-13 | D | **Done.** Posts on the landing page + `/posts`, net-new. `ContentService` gains `getFeaturedPosts(limit?)` (mirrors `getFeaturedProjects`) and `getPosts()` (mirrors `getProjects()` — unlimited, all published, used by the index page and `generateStaticParams` instead of a magic-number `getLatestPosts(1000)`); `Post` type gains `bodyMarkdown: string` and `featured: boolean` (both DB columns already existed, just weren't exposed to the read path); both `db-content.ts` and `local-content.ts` implement all three new/changed methods. `FeaturedPost` and `RecentPosts` (`features/posts/`) are Server Components — no `"use client"`, no new client JS — mounted on `/` between `Mission` and `Evidence`; both self-hide entirely (return `null`) when there's nothing to show (LAW-008). `/posts` (index, reuses the pre-existing but previously-unused `PostRow` component) and `/posts/[slug]` (detail, `generateStaticParams`/`dynamicParams=false` mirroring `/projects/[slug]`) are new routes; `ROUTES.posts` added to `BUILT_ROUTES` — nav lane and footer link appear automatically via the existing mechanism, no hand-edits to `nav-shell.tsx`/`footer.tsx` needed. `bodyMarkdown` renders server-side only via `marked` (new dependency, ~35 kB unpacked, never imported from a client file) through a small hand-styled `[&_h2]:...`/`[&_p]:...` wrapper rather than the `@tailwindcss/typography` plugin, since that plugin would import its own type scale and violate the closed-design-system rule (docs/30 §2.7.4) without a `DECISIONS.md` entry. Verified against real data in the local Postgres (`deepakai-db-1`): inserted 6 real published posts via direct SQL (1 featured, dated 1–6 days apart) — confirmed the Featured block renders the featured one with working markdown (`<h2>`, `<li>`, `<blockquote>` all real HTML), confirmed Recent shows exactly the 5 newest (`d058-verify-featured` + `recent-2..5`) and specifically excludes the 6th/oldest, confirmed `/posts` lists all 6. Deleted the fixtures and re-verified the zero-posts case: `Read the post` (Featured) and `Recent posts` heading both absent from `/`, `/posts` shows the honest `EmptyState`, `/` still 200s. Confirmed `/posts/[slug]` 404s for a draft post and for a nonexistent slug. `npm run typecheck --workspace=web` clean. `npm run check:bundle --workspace=web`: `/` First Load JS 151.7 kB (identical to the Phase B baseline — the new sections cost zero client bytes since they're pure Server Components reusing the already-loaded `motion`/`ScrollReveal` chunk), still ≤170 kB, ~18 kB headroom left for Phases E/F. `CONTENT_SOURCE=file npm run build` (the CI/file-fallback path, where `content/site.ts`'s `posts` array is empty) succeeds — 21/21 static pages, `/posts` and `/posts/[slug]` build cleanly against zero posts. Also fixed a stale commit-hash reference in this table's own Phase C row (`b0820d3`, an unreachable pre-amend object — the real commit is `2fbe8c5`). | Commit `994bd9c`. Dev server (left running from Phase C verification) stopped before building, per §8.3 — confirmed via `netstat`/`curl` before and after. |
| 2026-08-13 | D (real-world fixes) | **Owner published the first real post and hit three genuine bugs Phase D's own verification hadn't caught** — all found via Vercel production logs, not guesswork, all reproduced locally before shipping the fix. (1) `publishPost`/`publishProject` validated the submitted `question` but never wrote it — the `UPDATE` only touched `status`/`publishedAt`, so publishing straight after typing (without a separate "Save draft" first) hit Postgres's own `ck_published_has_question` constraint. Reproduced against local Postgres (old shape fails, fixed shape with `question` in the same `UPDATE` succeeds), fixed in both actions. (2) `/posts/[slug]` had `dynamicParams = false` (copied from `/projects/[slug]`) — hard-404s any slug not known at the last build regardless of DB state; this is what actually produced the reported 404. Removed it. (3) `publishPost`/`unpublishPost`/`archivePost`/`restorePostVersion` only called `revalidatePath` on admin routes, never on `/`, `/posts`, `/posts/[slug]` — so even with #2 fixed, list pages stayed frozen until the next deploy. Added the missing public-path revalidation to all four. Separately, **owner asked for a LinkedIn-style image treatment** after comparing the live post row against their actual LinkedIn post screenshot — root cause: Phase D's read path never joined `content_media` for posts at all (`coverImage` was declared on `Post` but never populated), unlike `/projects/[slug]`'s existing `loadItemMedia` usage. Added a batch `loadCoverImages` helper (one query, no N+1, mirrors the file's existing `rels` batching) to `getFeaturedPosts`/`getPosts`/`getLatestPosts`, reused the existing `loadItemMedia` for singular `getPost`. UI: `PostRow` gained a small thumbnail (self-hides without a cover — no placeholder box), `FeaturedPost` gained a full-width image below the text (LinkedIn's actual layout order: text, then image — no avatar/like-counts, since this is a single-author site with no real engagement data to honestly show), `/posts/[slug]` gained the cover-image block `/projects/[slug]` already had. Also fixed `PostRow` showing "0 min" instead of self-hiding when `readingMinutes` is unset (LAW-008). Verified against the owner's real "summer-intership" post (built directly against production Neon data, not simulated): rendered `<img>` carries the real R2 URL/alt text/dimensions on `/`, `/posts`, and `/posts/summer-intership`; zero "0 min" occurrences anywhere. `npm run check:bundle --workspace=web`: `/` First Load JS 156.8 kB (was 151.7 kB — `next/image` newly reachable from `/` costs ~5.1 kB, measured not assumed), still ≤170 kB, ~13 kB headroom left for Phases E/F. `npm run typecheck --workspace=web` clean; `CONTENT_SOURCE=file npm run build` still succeeds. | Commits `5498f60` (question-on-publish), `aa39f85` (revalidation + dynamicParams), `c64d66d` (cover images + 0-min) — all pushed same session, each deploy confirmed Ready and spot-checked live before moving to the next. |
| 2026-08-13 | D (carousel redesign) | **Done.** Owner researched how LinkedIn's feed actually works and gave an exact UI spec: Featured Posts and Posts both become horizontal-scroll carousels (arrows on desktop, native swipe on mobile), each card is image-top/title/preview/"Read more →", Featured ends in a "See all posts" card, and reading a post stays a normal vertical page — never a carousel. Clarified two ambiguities with the owner before touching code (both confirmed): (1) Featured Posts now allows **several** featured posts, ordered — not the single-boolean "one at a time" rule from Phase C; (2) Posts is a **second landing-page block** below Featured, not a redesign of `/posts` itself (which stays a plain full list — the "see everything" page, where a carousel would hide content). Schema: `postsTable.featured boolean` → `featuredOrder integer` (null = not featured, integer = carousel position). Generated as two separate migrations (`0007` purely additive, `0008` a pure drop) because `drizzle-kit generate` needs a TTY to disambiguate a same-table add+drop as rename-vs-recreate, which isn't available here — splitting the change into two unambiguous steps avoided that entirely. `savePost` now computes the order server-side: unchecking clears it, checking an already-featured post keeps its position, newly featuring one appends to the end (`max(featured_order)+1` in the same transaction) — no manual reordering UI, that's future scope if ever asked for. `getFeaturedPosts` has no artificial limit by default (ordered by `featuredOrder asc`, not date) and `restorePostVersion` degrades old boolean-shaped snapshots to "not featured" rather than guessing a position. New `Carousel` (`components/content/carousel.tsx`, client — scroll-snap + swipe native, arrow buttons the only JS) and `PostCard` (`components/content/post-card.tsx`, server) components; `FeaturedPosts` (renamed from the old singular `FeaturedPost`) and `RecentPosts` both rebuilt on top of them. Verified against real data built directly against production Neon (not simulated): 3 featured test posts rendered in exact `featuredOrder` sequence, "See all posts" trailing card present, 2 recent (unfeatured) posts filled the Posts carousel correctly ordered by date, the owner's real `summer-intership` cover image still rendered inside its card, `/posts` confirmed unchanged (plain list, zero carousel markup) — all via real `<a href>`/class-name assertions against the built HTML, not text-matching noise from the RSC payload. Deleted the test posts after. `npm run check:bundle --workspace=web`: `/` First Load JS 157.2 kB (was 156.8 kB — the new `Carousel` client component costs ~0.4 kB), still ≤170 kB, ~13 kB headroom left for Phases E/F. `npm run typecheck --workspace=web` clean; `CONTENT_SOURCE=file npm run build` still succeeds. | Commit `bd442f9`. Migrations `0007_sleepy_odin.sql` / `0008_jazzy_shiver_man.sql` applied to both local Postgres and Neon in this session. |
| 2026-08-13 | E | **Done.** Timeline section, net-new — but the original spec's ordering mechanism (chronological, via `start_date`/`end_date`/`year`) turned out to be unbuildable as written: every project's `start_date`/`end_date` is NULL, `year` is 2026 on all 6 published ones, and `projects.txt` (the plan's cited date reference) doesn't exist in the repo. Researched external practice first (NN/g on zigzag-layout usability, CSS `animation-timeline: view()` support, timeline UI a11y patterns) before proposing a checklist, per owner's explicit request — findings: zigzag layouts measurably hurt scanning *for decorative images*, but NN/g's own exception (informational content, short lists) covers this case; vertical timelines should be a semantic `<ol>` of links, not a tablist (that's the horizontal-timeline pattern). Owner resolved the blocker directly: order by an owner-assigned `timelineOrder` instead of dates, no dates rendered at all (fabricating a month from `published_at`'s literal 1st-of-six-consecutive-months pattern would violate LAW-008), same info already on the project (title/problem/status, nothing new), click → `/projects/[slug]`. Schema: `projectsTable.timelineOrder integer` nullable, migration `0009_light_logan.sql` (clean single-step generate — pure additive, no rename ambiguity). `ContentService.getTimelineProjects()` — published, `timelineOrder IS NOT NULL`, ordered ascending; named distinctly from the pre-existing, unrelated `getTimeline()` (career `timeline_entries`, 0 rows, different table) to avoid confusion. Admin: "Timeline position" numeric field added to `ProjectEditor.tsx` — routed around the file's known pre-existing aside-outside-form bug (flagged, not fixed, in the Phase C entry) by giving the new field its own hidden proxy input inside the `<form>`, the same working pattern `featured`/`verified` already use, rather than adding to the broken aside directly. Also pre-emptively added `revalidatePath("/")` to `saveProject`/`publishProject`/`unpublishProject`/`archiveProject`/`restoreVersion` — the exact gap that caused the Phase D revalidation bug, fixed here before it could recur rather than after. `ProjectTimeline` (`features/timeline/project-timeline.tsx`) is a Server Component, zero client JS: CSS Grid zig-zag, mobile-first (single column + left spine below 768px, alternating two-column at md+ via `:nth-child`, no `display: contents` so the `<li>` keeps a normal exposed box for AT), scroll-driven reveal via `animation-timeline: view()` mirroring the gallery cluster's exact structure (base style *is* the final/reduced-motion/unsupported-browser state; the entrance animation is additive only inside `@media (prefers-reduced-motion: no-preference) { @supports (animation-timeline: view()) { ... } }`, so there's no separate override to remember or forget). Mounted between `RecentPosts` and `Evidence` — **`Evidence` was deliberately not deleted**: the owner said "start the work" but never confirmed the deletion specifically, and `docs/30` itself flags that as a not-reversible-by-accident call, so it stayed (Open Question 1 resolved as "kept," not "deferred"). Verified against real data, built directly against production Neon (not simulated): assigned `timelineOrder` 0/1/2 to `asmos`/`pesu-vault`/`turb-detr` — deliberately out of `published_at` order — and confirmed the rendered HTML sequence is `asmos → pesu-vault → turb-detr` (proving `timelineOrder`, not date, actually drives it), confirmed the `<a href>`s point at `/projects/[slug]` correctly, confirmed active/archived badges render correctly (1 active, 2 archived). Cleared the test positions and rebuilt: confirmed the section vanishes entirely (`timeline-list`/heading both absent) with zero timeline projects. `npm run check:bundle --workspace=web`: `/` First Load JS 157.2 kB — **identical** to the pre-Timeline measurement, 0 kB added, as expected for a pure Server Component with CSS-only motion. `npm run typecheck --workspace=web` clean; `CONTENT_SOURCE=file npm run build` succeeds (incidentally exercises the zero-timeline-projects case too, since none of the 18 file-based fixtures have `timelineOrder` set). Also noticed in passing: the owner has been actively publishing real posts since Phase D shipped (15 published by this build, not just the one test post) — a good incidental sign the Phase D fixes are holding up under real use. | Commit `3efc3cd`. Migration `0009_light_logan.sql` applied to both local Postgres and Neon. |
| 2026-08-13 | E (real data corruption + fixes) | **Owner reported "can't reopen after save" and "you made all worse."** Investigated via production logs + direct Neon queries, not guesswork. Found: `ProjectEditor.tsx`'s long-flagged aside-outside-form bug (Phase C entry, never fixed) was silently writing `slug=""`, `year=0`, `projectStatus="archived"`, `tags=[]` on every `saveProject` call, because those four fields live in the aside (outside the `<form>`) with no hidden-proxy input — unlike `featured`/`verified`, which already had the workaround. Confirmed `asmos` had actually been corrupted this way (real production data, not a hypothetical): `slug=''`, `year=0`, `project_status='archived'` (was `'active'`), `tags=[]` (was `['Python']`) — which is exactly why it couldn't be reopened (`/admin/projects/asmos` queries `WHERE slug='asmos'`, found nothing). Also explains why *other* saves that session looked fine: once one project held `slug=''`, every later save's uniqueness check found `""` already taken and aborted before writing anything — failed closed, not further corruption, but also no further saves succeeded. **Fixed at the source**: added hidden-proxy inputs for `slug`/`year`/`projectStatus`/`tags` inside the actual `<form>` (same pattern `featured`/`verified`/`timelineOrder` already used), renamed the aside's visible inputs to avoid a name collision. Restored `asmos`'s real values directly from a query run earlier in this same session (before the corruption), since its own `content_versions` history had nothing pre-corruption to restore from. Confirmed zero other projects hold `slug=''`. Separately: owner clarified the Timeline node should be a **glowing sphere** matching the hero neural-network's bulb nodes, not a hollow ring — rebuilt `.timeline-dot` as a radial-gradient core (white → `--grad-1` → `--grad-2` → `--grad-3`, the hero's own accent gradient) plus a layered `box-shadow` bloom halo, CSS-only (three.js/postprocessing stay banned from `/`'s bundle — this reproduces the look, not the engine). And: owner sent a screenshot of `Evidence` and said "remove the above section" — deleted it from the landing page for real this time (resolves Open Question 1; the earlier same-day Phase E entry's "kept, not deleted" is superseded). Verified: rebuilt against real Neon data after the fix, confirmed `asmos.html` generates again (slug reachable), confirmed `Evidence`'s heading text is gone from `/`, confirmed the compiled CSS carries the new gradient/box-shadow rules. `check:bundle` unchanged at 157.2 kB. `npm run typecheck --workspace=web` clean; `CONTENT_SOURCE=file npm run build` succeeds. | Commit `b28427b`. Data restored directly on Neon via a one-off script, not a migration — no schema change. |
| 2026-08-13 | E (node redesign) | Owner: "again wrong design… the node in timeline is a glowing sphere… make the sphere bigger and reactive to the mouse operations." Removed the `problem` text from each node — at real content length it was a ~900-character wall (ASMOS's), and the spine is a map, not a summary; detail already lives one click away on `/projects/[slug]`. Also dropped the active/archived badge, reading "just name" literally. Sphere doubled (0.875rem → 1.75rem) with the specular highlight moved off-centre so it reads as a sphere rather than a flat disc. Mouse-reactive in pure CSS (zero client JS): `:hover` swells to 1.32 and intensifies the bloom, `:active` presses back to 1.1, driven from the whole node so pointing anywhere on the row lights its sphere; keyboard parity via `:has(.timeline-card:focus-visible)`; under `prefers-reduced-motion` the swell is dropped but the brightening (a colour change, not motion) is kept. **Fixed a real defect found while doing this:** the sphere sat *outside* the `<a>`, so clicking it did nothing — the link's `::after` overlay now covers the whole node. The underline sweep moved to an inner `<span>` because `.gradient-underline-hover` also uses `::after` and the two collide on one element (same structure `ProjectCard` already uses). **Also fixed a latent bug shipped in `3efc3cd`:** the spine used `var(--border)`, which does not exist in this codebase — the token is `--border-default`, so the spine had been rendering with an invalid colour. Verified against real Neon data with four projects assigned positions (rendered order matched `timelineOrder`, node markup was sphere + name only, click overlay present on every link), and verified **in the compiled, minified CSS** that the reduced-motion `scale:1` reset stayed scoped inside its media query — a leak there would have silently killed the hover swell for everyone — and that the `:has()` rule survived minification. Test positions reverted afterward. `check:bundle` unchanged at 157.2 kB (CSS-only change). | Commit `9877e1d`. |
| 2026-08-13 | F | **Done — but the phase's stated premise was wrong and had to be corrected first.** `docs/30` described Phase F as blocked on an owner alt-text pass for 11 photos. Verified against production before writing code: **`gallery_items` had 0 rows** — those 11 photos live only in `content/gallery.ts` (file mode), while production runs `CONTENT_SOURCE=db`, so removing the `notFound()` guard alone would have shipped an empty page. Meanwhile the media library held 19 images, **all with alt text and dimensions** (media requires alt on upload). The real gap was never copy — it was that nothing could *put* an image into the gallery, which is exactly what the owner asked for. Built `actions/gallery.ts` (`addToGallery`, `saveGalleryItem`, `removeFromGallery`, `moveGalleryItem`) and `/admin/gallery` + `GalleryManager`: a picker of media images not yet in the gallery, then per-item editing of alt/caption/info/place/date/time, the layout knobs (`size`/`tilt`/`depth`), order (↑/↓, which rewrites the whole run 0..n-1 and so also repairs gapped/duplicate `sortOrder` from earlier edits), and Published/Featured. Bridged the media→`gallery_items` gap: both `gridKey`/`fullKey` point at the single media object (`next/image` already serves a right-sized rendition per breakpoint, so a second stored copy buys nothing), and `blurData` is generated with `sharp` at add-time — it is `notNull` with no default and all three gallery components feed it to `placeholder="blur"`, which throws on an empty value, so it could not be deferred. `sharp` was already proven on this runtime path (`lib/media/validate.ts` via `uploadMedia`). **Found and fixed a correctness bug that would have leaked drafts publicly:** `services/gallery.ts` returned *every* `gallery_items` row regardless of `published` — the flag appeared nowhere in the read path — so the new Published toggle would have been decorative and unpublished items would have gone live the instant the guard came off. Now filtered. Went live per owner decision: production `notFound()` guard removed, `GalleryStrip` mounted after Timeline, `ROUTES.gallery` added to `BUILT_ROUTES` (footer link appears via the existing mechanism). `/gallery` renders an honest `EmptyState` at zero photos rather than 404ing, matching `/posts` — a footer link that 404s is a fabricated affordance (LAW-008). Verified end-to-end against **real production data**, not simulated: fetched a real R2 image over its public URL, ran the exact sharp pipeline (36,715 bytes → valid 95-char webp data URL), inserted a real `gallery_items` row, rebuilt, and confirmed it rendered on both `/gallery` (correct singular "1 photograph" copy) and the landing cluster with the real R2 key. Then flipped that row to `published=false`, rebuilt, and confirmed it appeared **nowhere** — `/gallery` fell back to the empty state and the landing strip self-hid — proving the new filter works rather than assuming it. Test row deleted afterward; `gallery_items` back to 0. `check:bundle` 157.2 kB unchanged, and its `sharp` assertion confirms the new server-action import stayed out of `/`'s client bundle. `npm run typecheck --workspace=web` clean; `CONTENT_SOURCE=file npm run build` succeeds. **Not verified locally:** the credentialed R2 `GetObject` call inside `addToGallery` — `vercel env pull` returns masked placeholders for Sensitive vars, so the public-URL fetch was substituted for the byte-fetch step. `putObject` already runs in this same server-action runtime with the same client and config, so the risk is low, but the first real "Add to gallery" click is the true test. | Commit pending (this session). No migration — `gallery_items` already existed and was unchanged. |

---

## 8. Working discipline (non-negotiable)

1. **One phase at a time.** Do not start E while B is unverified.
2. **Verify before claiming.** Every "done" needs command output or a browser
   observation. If something could not be verified, say so explicitly and why.
3. **Never run a production build while a dev server is running** — it corrupts
   `.next` and produces confusing stale-state failures. This has already
   happened twice in this project.
4. **Diagnose before editing.** Phase A in particular starts with measurement;
   the two mobile hypotheses are plausible but unconfirmed.
5. **Commit per phase**, with the acceptance gate results in the commit body.
6. **Owner review gates**: hero motion, timeline design, and all public copy
   are the owner's call, not the implementer's.
